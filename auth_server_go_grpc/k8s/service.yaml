kind: Service
apiVersion: v1
metadata:
  labels:
    discovery.solo.io/function_discovery: disabled
  name: auth-server
  namespace: gloo-system
spec:
  selector:
    app: auth-server
  ports:
  - name: grpc
    protocol: TCP
    port: 8000
    targetPort: 8000
